extends layout_default

block contents
	article.container
		header.row
			.col-12
				h2& New User
		section.row
			form(method="POST")
				include users__form, user
				.mb-3
					button.btn.btn-secondary(onclick="javascript::window.close()")& Cancel
					button.btn.btn-primary(type="submit")& Confirm
