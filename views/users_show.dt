extends layout_default

block contents
	article.container
		header.row
			.col-12
				h2& User
		section.row
			p #{user.username}
			p #{user.password}
			p #{user.email}
			.mb-3
				button.btn.btn-secondary(onclick="javascript::window.close()")& Back
